CREATE TABLE cdr(callingNumber VARCHAR, LAT VARCHAR, CGI VARCHAR, LON VARCHAR, MNC VARCHAR, LAC VARCHAR, IMSI VARCHAR, MCC VARCHAR, CID VARCHAR, recordType VARCHAR, eventTime VARCHAR, calledNumber VARCHAR);
CREATE TABLE connections_with_confidence(maid_id VARCHAR, phone_number VARCHAR, distance DOUBLE, maid_timestamp VARCHAR, cdr_timestamp VARCHAR, confidence VARCHAR);
CREATE TABLE maid(ALTITUDE VARCHAR, CELL_IDENTITY VARCHAR, CONNECTION_METHOD VARCHAR, COURSE VARCHAR, CQI VARCHAR, DBM VARCHAR, DERIVED_COUNTRY VARCHAR, DEVICE_MAKE VARCHAR, DEVICE_MODEL VARCHAR, DEVICE_OS VARCHAR, DEVICE_OS_VERSION VARCHAR, FLOOR VARCHAR, FOREGROUND VARCHAR, GATEWAY_LATITUDE VARCHAR, GATEWAY_LONGITUDE VARCHAR, GEOHASH VARCHAR, HEADING VARCHAR, HORIZONTAL_ACCURACY VARCHAR, ID VARCHAR, ID_TYPE VARCHAR, IP_ADDRESS VARCHAR, LATITUDE VARCHAR, LONGITUDE VARCHAR, OPERATOR_NAME VARCHAR, RSRP VARCHAR, RSRQ VARCHAR, RSSNR VARCHAR, SPEED VARCHAR, "TIMESTAMP" VARCHAR, USER_LOCALE VARCHAR, VERTICAL_ACCURACY VARCHAR);
